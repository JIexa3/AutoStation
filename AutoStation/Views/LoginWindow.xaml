<Window x:Class="AutoStation.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:AutoStation.Views"
        xmlns:controls="clr-namespace:AutoStation.Controls"
        mc:Ignorable="d"
        Title="Вход в систему" Height="540" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Логотип -->
        <controls:LogoControl HorizontalAlignment="Left" 
                            VerticalAlignment="Center" Grid.Column="0"/>

        <!-- Форма входа -->
        <StackPanel Grid.Row="1"
                    VerticalAlignment="Top" Margin="0,80,0,0" Grid.ColumnSpan="2">
            <TextBlock Text="Вход в систему" 
                       Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                       Margin="0,0,0,32"/>

            <TextBox x:Name="UsernameTextBox"
                     materialDesign:HintAssist.Hint="Имя пользователя"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="32,0"/>

            <PasswordBox x:Name="PasswordBox"
                        materialDesign:HintAssist.Hint="Пароль"
                        Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                        Margin="32,16"/>

            <Button Content="Войти"
                    Click="LoginButton_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="{DynamicResource PrimaryHueDarkBrush}"
                    BorderBrush="{DynamicResource PrimaryHueDarkBrush}"
                    Foreground="White"
                    materialDesign:ButtonAssist.CornerRadius="8"
                    Height="45"
                    FontSize="14"
                    FontWeight="Medium"
                    Margin="0,16"/>

            <Button Content="Регистрация"
                    Click="RegisterButton_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Height="45"
                    FontSize="14"
                    Margin="0,8"/>
        </StackPanel>
    </Grid>
</Window>
