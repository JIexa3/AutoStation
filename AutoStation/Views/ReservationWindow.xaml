<Window x:Class="AutoStation.Views.ReservationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Бронирование колонки" Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Бронирование колонки"
                 Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                 Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <ComboBox x:Name="ColumnComboBox"
                     materialDesign:HintAssist.Hint="Выберите колонку"
                     Style="{StaticResource MaterialDesignOutlinedComboBox}"
                     Margin="0,0,0,8">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Number, StringFormat='Колонка {0}'}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <DatePicker x:Name="ReservationDatePicker"
                      materialDesign:HintAssist.Hint="Выберите дату"
                      Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                      Margin="0,0,0,8"/>

            <ComboBox x:Name="TimeComboBox"
                     materialDesign:HintAssist.Hint="Выберите время"
                     Style="{StaticResource MaterialDesignOutlinedComboBox}"
                     Margin="0,0,0,8">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding StringFormat={}{0:HH:mm}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <GroupBox Grid.Row="2"
                  Header="Текущие бронирования"
                  Style="{StaticResource MaterialDesignGroupBox}">
            <DataGrid x:Name="ReservationsDataGrid"
                      Grid.Row="1"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Margin="0,16,0,0"
                      Style="{StaticResource MaterialDesignDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Колонка"
                                      Binding="{Binding FuelColumn.Number}"
                                      Width="Auto"/>
                    <DataGridTextColumn Header="Время начала"
                                      Binding="{Binding ReservationTime, StringFormat={}{0:dd.MM.yyyy HH:mm}}"
                                      Width="*"/>
                    <DataGridTextColumn Header="Время окончания"
                                      Binding="{Binding ExpirationTime, StringFormat={}{0:dd.MM.yyyy HH:mm}}"
                                      Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Отмена"
                    Click="CancelButton_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,8,0"/>
            <Button Content="Забронировать"
                    Click="ReserveButton_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="{DynamicResource PrimaryHueDarkBrush}"
                    BorderBrush="{DynamicResource PrimaryHueDarkBrush}"
                    Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}"/>
        </StackPanel>
    </Grid>
</Window>
